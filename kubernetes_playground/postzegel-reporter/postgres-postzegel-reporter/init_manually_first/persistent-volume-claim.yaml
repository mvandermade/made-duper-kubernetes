apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-postzegel-reporter-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
  volumeName: postgres-postzegel-reporter-persistent-volume
