apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-postzegel-reporter
  labels:
    app: postgres-postzegel-reporter
spec:
  selector:
    matchLabels:
      app: postgres-postzegel-reporter
  template:
    metadata:
      labels:
        app: postgres-postzegel-reporter
