apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-postzegel-reporter-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
  volumeName: kafka-postzegel-reporter-persistent-volume
